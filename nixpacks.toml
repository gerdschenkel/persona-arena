[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "gunicorn persona_arena_app:app --worker-class gthread --workers 1 --threads 4 --timeout 120"
